require "test_helper"

class EventosControllerTest < ActionDispatch::IntegrationTest
  # setup do
  #   @follower = followers(:one)
  #   @user = users(:one)
  #    post users_url, params: { email: @user.email, image: @user.image, language: @user.language, location: @user.location, login_token: nil, name: @user.name, nif: @user.nif, password: @user.password_digest, password_confirmation: @user.password_digest, phone: @user.phone, role: @user.role, username: @user.username }, as: :json
  #   @user_c = users(:two)
  #   post users_url, params: { email: @user.email, image: @user.image, language: @user.language, location: @user.location, login_token: nil, name: @user.name, nif: @user.nif, password: @user.password_digest, password_confirmation: @user.password_digest, phone: @user.phone, role: @user.role, username: @user.username }, as: :json
  # end
  #
	# test "should create follower" do
  #   assert_difference('Follower.count') do
  #     post followers_url, params: { token: @user_c.login_token, follower: { company_id: @follower.company_id, customer_id: @follower.customer_id}}, as: :json
  #   end
  # end
  #
  # test "should destroy follower" do
  #   assert_difference('Follower.count', -1) do
  #     delete follower_url(@evento), params: {token: @user_c.login_token, follower: { company_id: @follower.company_id, customer_id: @follower.customer_id}}, as: :json
  #   end
  # end

end
